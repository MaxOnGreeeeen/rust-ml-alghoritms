mod k_means;
mod knn;
mod decision_tree_cart;

fn main() {
    // k_means::benchmark_k_means();
    // knn::benchmark_knn();
    // knn::knn_parallel();
    // knn::benchmark_knn_parallel();
    // decision_tree::tree_decision();
    // decision_tree_cart::make_prediction();
}